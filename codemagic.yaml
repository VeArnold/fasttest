workflows:
  my-workflow:
    name: first
    environment:
      groups:
        - esimene
        - teine
        - kolmas
      vars:
        WHY: 'catcatcat'
    scripts:
      - echo $JAVA_HOME
      - echo $KASS
      - echo $TASS
      - echo $WHY
    artifacts:
      - testing/*.aab
    publishing:
      firebase:
        firebase_token: $TOKEN
        android:
          app_id: $FIREBASE_ANDROID_ID
          groups:
            - magicGroup
